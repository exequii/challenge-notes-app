type Note = {
    id: string,
    title: string,
    lastEdit: string,
    content: string,
    categories: string[],
    archived: boolean
  }
  //Defino las caracteristicas que tendra una Nota

export default Note